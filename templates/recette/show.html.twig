{% extends "base.html.twig" %}

{% block body %}

     
     <div class="container">
          <div class="jumbotron">
               <h2>{{ recette.titre }}</h2>
          </div>

          <div class="row mt-3">
               <div class="col-md-3">{{ recette.temps }}</div>
               <div class="col-md-3">{{ recette.personne }}</div>
               <div class="col-md-3"></div>
               <div class="col-md-3"></div>
          </div>

          <div class="row mt-4">
               <div class="col-md-3">
               </div>
               <div class="col-md-9">
                    {{ recette.preparation }}
               </div>
          </div>
     </div>
     
{% endblock %}